# Literary Showcase - Example environment file
# Copy to .env.local for local dev or configure in Vercel Project Settings

# Database (required in Vercel)
# Use the same connection string for both if you don't use separate pooled vs direct URLs.
DATABASE_URL="postgres://USER:PASSWORD@HOST:5432/DB?sslmode=require"
DIRECT_URL="postgres://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# Optional: Prisma Accelerate (runtime-only) - leave empty if unused
# PRISMA_DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_ACCELERATE_API_KEY"

# Auth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="please-set-a-strong-secret"

# Admin bootstrap (optional)
ADMIN_EMAIL=""
ADMIN_PASSWORD=""

# AI Providers (optional)
OPENAI_API_KEY=""
GEMINI_API_KEY=""
DEEPSEEK_API_KEY=""

# OCR.space (optional)
OCR_SPACE_API_KEY=""
OCR_SPACE_ENDPOINT="https://api.ocr.space/parse/image"

# Node env
NODE_ENV=development